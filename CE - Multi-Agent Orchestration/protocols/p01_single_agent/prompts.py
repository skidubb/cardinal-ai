"""Prompts for P01 Single Agent.

Intentionally minimal — the agent's own system prompt (from Agent Builder's
_ROLE_PROMPTS) handles role-specific behavior. P01 is a thin protocol wrapper.
"""

# Empty by design — no orchestration-level prompt needed.

# Cardinal Element Streamlit Demo — Environment Configuration
# Copy this to .env or .env.local and fill in your API keys

# Required: Anthropic API Key for Claude models
ANTHROPIC_API_KEY=sk-your-key-here

# Optional: If running from demo/ directory and main src/ is a parent directory,
# the app will automatically find it. No need to modify PROJECT_ROOT.
